Command to run: sudo python mini1.py

Created a custom network topology using mininet python APIs

. All the switches are connected to each other 
. Odd hosts can only talk to each other and even hosts can talk to each other 
. The communication between odd hosts is at 1 mbps and for even hosts at 2 mbps 

